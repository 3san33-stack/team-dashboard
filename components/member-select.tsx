"use client";
import {ArrowUpRight,ChevronRight} from 'lucide-react';
import {MEMBERS,type Member} from '@/lib/types';
import {StudioFilm} from '@/components/studio-film';
export function MemberSelect({onSelect}:{onSelect:(member:Member)=>void}){
 return <main className="studio-entry"><section className="studio-entry-story"><StudioFilm className="studio-entry-film"/><div className="studio-entry-overlay"/><img className="studio-entry-logo" src="/songwol-logo.png" alt="SONGWOL"/><div className="studio-entry-copy"><span>DESIGN & RESEARCH</span><h1>함께 상상하고,<br/>새롭게 만들어갑니다.</h1><p>매일의 기록이 모여<br/>송월의 다음 이야기가 됩니다.</p><div className="studio-entry-chapter"><span>OUR EVERYDAY STUDIO</span><span>01 — ∞</span></div></div></section><section className="studio-entry-select"><div className="studio-entry-meta">SONGWOL TEAM WORKSPACE <ArrowUpRight size={17}/></div><div className="studio-entry-form"><span className="studio-eyebrow">WELCOME BACK</span><h2>오늘도, 반가워요.</h2><p>이름을 선택하고 나의 작업을 이어가세요.</p><div className="studio-member-list">{MEMBERS.map((member,i)=><button key={member} className="studio-member-card" style={{animationDelay:`${i*60}ms`}} onClick={()=>onSelect(member)}><span className={`studio-person studio-person-${i}`}>{member[0]}</span><span><strong>{member}</strong><small>디자인 R&D팀</small></span><ChevronRight size={19}/></button>)}</div><p className="studio-entry-hint">선택한 이름은 이 브라우저에 기억됩니다.</p></div><div className="studio-entry-footer">작은 아이디어에서, 더 나은 일상으로.</div></section></main>
}
